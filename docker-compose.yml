services:
  jekyll:
    image: jekyll/jekyll:4
    command: jekyll serve --watch --drafts --force_polling
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
      - jekyll_bundle:/usr/local/bundle

volumes:
  jekyll_bundle:
